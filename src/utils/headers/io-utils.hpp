#ifndef IO_UTILS_HPP
#define IO_UTILS_HPP

#include <vector>
#include <string>

using namespace std;

int prompt_menu(vector<string> options, bool clear_screen, bool show_exit_option = true);

#endif
